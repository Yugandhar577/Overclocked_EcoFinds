<% layout('layouts/boilerplate') %>

<h1 class="mt-4 text-center">Welcome to Eco Finds</h1>

<div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1 mt-4">
  <% if (listings.length === 0) { %>
    <div class="col-12 text-center">
      <p class="text-muted">No listings available at the moment.</p>
    </div>
  <% } else { %>
    <% listings.forEach(listing => { %>
      <div class="col mb-4">
        <div class="card h-100 shadow-sm">
          <img src="<%= listing.images %>" alt="<%= listing.title %>" style="max-width: 300px;" class="card-img-top mx-auto mt-3">
          <div class="card-body">
            <a href="/listings/<%= listing.id %>"><h5 class="card-title"><%= listing.title %></h5></a>
            <p class="card-text"><%= listing.description %></p>
          </div>
          <div class="card-footer">
            <p class="mb-0"><strong>₹<%= listing.price %></strong></p>
            <p class="mb-0 text-muted">Condition: <%= listing.condition %></p>
          </div>
        </div>
      </div>
    <% }) %>
  <% } %>
</div>
